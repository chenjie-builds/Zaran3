# Zaran 计算流体力学程序技术文档

> 程序名：Zaran —— A Totally Automatic CFD Solver（全自动计算流体力学求解器）
> 作者：Chen Jie（陈洁）
> 版本：3.10.1（CMakeLists 中 `PROJECT_VERSION`，二进制名为 `Zaran3.10.1.exe`）
> 语言/标准：C++17，CMake（≥3.21），Visual Studio 2019/2022
> 并行：OpenMP（`USE_OMP` 宏，默认开启）
> 第三方库：Eigen3、spdlog、VTK（9.3）、CGNS、HDF5、TecIO（Tecplot）、span-lite

---

## 1. 概述

Zaran 是一个面向可压缩流动（Euler / Navier-Stokes 方程）的全自动 CFD 求解器。其核心特色是基于作者博士工作的一系列 **非结构网格有限差分方法** 与 **扎染（Zaran）算法**：

1. **自由节点有限差分法（FNFDM, Flexible Node FDM）**：在非结构点云/混合网格上，通过“局部三相邻节点”构造当地坐标变换（ξ/η/ζ 三根当地网格线），从而在无网格拓扑约束的情况下做有限差分。
2. **结构网格有限差分法（Structured FDM）**：在贴体结构网格上求解 NS 方程，提供多种度量系数（S0/S1/S2/S3）、半点插值（一阶/MUSCL/MUSCL_SV/WCNS5/Grad）与通量差分（2/4/6 阶）格式，并实现了 **DEER**（离散等价方程及其离散等价准则）与 **CMM**（Consistent Metric Method，度量系数存于半点）两种度量计算方法。
3. **扎染算法（Zaran）**：以一块均匀正交的“主网格（Master/Block）”包围浸入的 STL/PLY 模型，自动标记流体/固体/过渡单元，并在物面附近生成一层“从属自由节点网格（Slave Field）”，通过场间数据通信耦合，实现浸入边界式的复杂外形自动计算。

程序采用“控制文件驱动 + 工厂创建 + 面向对象分层”的架构，将整个流程组织为：**读取控制文件 → 生成网格/场 → 求解器迭代 → 结果输出**。

---

## 2. 总体架构与目录结构

### 2.1 分层架构

程序按职责分为四个逻辑层：

| 层 | 目录 | 职责 |
|----|------|------|
| 应用层（Main） | `src/Main` / `inc/Main` | 程序入口、任务调度、求解主循环控制 |
| 生成器层（Generator） | `src/Generator` / `inc/Generator` | 根据控制参数创建网格与场（工厂模式） |
| 场与求解层（Zone） | `src/Zone` / `inc/Zone` | Field、Solver、DataManager、SolverParam 等核心计算对象 |
| 基础层（Basic） | `src/Basic` / `inc/Basic` | 网格数据结构、物理模型、黎曼求解器、数学工具、前后处理、通用工具 |

### 2.2 目录结构

```
Zaran3/
├── CMakeLists.txt           # 顶层构建脚本
├── CMakeSettings.json
├── README.md / change.md    # 说明与版本记录
├── inc/                     # 头文件
│   ├── Basic/
│   │   ├── Common/          # BasicType.h、CommonPara.h（枚举、常量、Box）
│   │   ├── Data/            # GlobalData.h（全局参数单例）
│   │   ├── Grid/            # 网格类（GridStruct、GridFNFDM、GridBlock、Node/Cell/Face、Bound、IdxProxy）
│   │   ├── Math/            # MathBasic、Limiter、LastSquare
│   │   ├── Model/           # Model、ModelManager、PolyData、PointCloudModel
│   │   ├── Physics/         # Gas、PerfectGas、Dimensionless
│   │   ├── PostProcess/     # Visual（Tecplot/VTK/CGNS 输出）
│   │   ├── PreProcess/      # GridConvert、ReadSTL、ReadPLY
│   │   ├── RiemannSolver/   # Roe/HLLC/AUSMPW/StegerWarming/VanLeer 及工厂
│   │   └── Utilities/       # File、KDTree、Log、Timer、SpecialField
│   ├── Generator/
│   │   ├── Field/           # FieldGenerator.h
│   │   └── Grid/            # 各类网格生成器
│   ├── Main/                # Application.h、FieldSimulation.h
│   └── Zone/
│       ├── Data/            # FieldData、FieldDataCommInfo
│       ├── DataManager/     # FieldDataManager、DataManagerNS(Struct)
│       ├── Field/           # Field、GlobalField、NSField(FN/Struct/Zaran)
│       ├── Solver/          # NSSolver 体系、Metric、GradWLSQ、Residual、TimeCompute
│       └── SolverParam/     # SolverPara、FlowSolverPara、FlowSolverStructPara、CFL
└── src/                     # 与 inc 对应的实现文件
    ├── Basic/  ├── Generator/  ├── Main/  └── Zone/
```

### 2.3 核心对象关系

```
main()
 └─ Application（读取 zaran.ini → 全局参数 GlobalData → 任务分派）
     └─ NSFieldSimulation（求解主循环控制器）
         └─ FieldManager（管理多个 Field）
             └─ Field（每个场 = 网格 + 求解器 + 求解参数 + 场数据 + 数据管理器）
                 ├─ GridBase ── GridStruct / GridFN / GridBlock
                 ├─ FieldSolver ── NSSolver ── NSSolverFNFDM / NSSolverStruct(→CMM/DEER→Block)
                 ├─ SolverParam ── FlowSolverParam ── FlowSolverParamStruct
                 ├─ FieldData（通用数据容器）
                 └─ DataManager ── DataManagerNS / DataManagerNSStruct
```

---

## 3. 程序入口与任务调度

### 3.1 `main.cpp` 入口

1. 确定工作目录与控制文件 `zaran.ini`：
   - 无命令行参数（`argc==1`）：使用程序当前目录下的 `zaran.ini`；不存在则报错退出。
   - 有参数（`argv[1]`）：以该路径为工作目录，控制文件为 `工作目录\zaran.ini`。
2. 初始化日志 `Logger::Start(work_dir)`。
3. 构造 `Application`（构造时初始化 `GlobalData` 单例并写入 `work_dir`）。
4. 调用 `app->Run()` 执行任务。
5. 结束后倒计时 10 秒再退出。

### 3.2 `Application::Run()` 流程

```
ReadGlobalData()   // 解析 zaran.ini → GlobalData
InitTask()         // 根据 simulationTask 确定任务类型
ShowInfo()         // 打印版本/任务信息，检查控制文件版本
分支：
  SOLVE_FIELD   → SolveField()   （流场求解，主流程）
  CONVERT_GRID  → ConvertGrid()  （网格格式转换）
  READ_MODEL    → ReadModel()    （模型读取/闭合性检查）
```

任务类型由 `TaskType` 枚举定义（`SOLVE_FIELD` / `CONVERT_GRID` / `READ_MODEL`），由控制文件 `simulationTask` 决定。

### 3.3 `ReadGlobalData()` — 控制文件解析

逐行读取 `zaran.ini`，跳过空行与 `!`、`#`、`/` 开头的注释行；按 `type name = value` 格式解析，`type` 为 `string`/`double`/`int`，通过 `GlobalData::Update(name, value)` 写入全局参数表。

`GlobalData` 是一个**单例**（`static map<string, dataVariant>`，`dataVariant = variant<string,int,double>`），提供 `GetInt/GetDouble/GetString`、`IsExist`、`Update`、`Backup` 接口。所有模块（求解器、生成器、控制器）均通过它读取全局参数。

### 3.4 `SolveField()` — 进入流场求解

1. 创建结果文件夹 `resultFolder` 与备份文件夹 `backupFieldFolder`。
2. 解析 `Dimension`（2/3 维）、`solverType`（当前仅支持 `NS`）、`gridType`：

| `gridType` | 网格类型 | 求解器类型（`FieldSolverType`） |
|------------|----------|-------------------------------|
| `Structured` | 结构网格 | `NS_Struct` |
| `Flexible` | 自由节点网格 | `NS_FNFDM` |
| `Zaran` | 扎染主/从网格 | `NS_ZaRan` |

3. 用 `FieldGenerator` 工厂创建 `FieldManager`（包含一个或多个 `Field`）。
4. 构造 `NSFieldSimulation` 控制器并调用 `SolveField()` 执行迭代。

---

## 4. 控制文件 `zaran.ini` 参数说明

控制文件为键值对文本，格式 `type name = value`。主要参数如下（以 `bin\Release\zaran.ini` 为准）：

| 参数名 | 类型 | 含义 | 备注 |
|--------|------|------|------|
| `version` | string | 控制文件版本（≥V3.3.7） | 版本过低会警告 |
| `simulationTask` | string | 任务类型 `SOLVE_FIELD`/`CONVERT_GRID`/`READ_MODEL` | |
| `Dimension` | int | 维数 2/3 | |
| `gridType` | string | 网格类型 `Structured`/`Flexible`/`Zaran` | |
| `solverType` | string | 求解器 `NS` | |
| `isViscous` | int | 是否粘性 1/0 | |
| `rkStage` | int | Runge-Kutta 阶数 1/2/3 | |
| `gradScheme` | int | 梯度方法 0=WLS 最小二乘，1=UFDM，-1=不计算 | |
| `isSteady` | int | 0=非定常（真实时间推进），1=定常 | |
| `metrics_scheme` | string | 结构网格度量系数 `S0`/`S1`/`S2`/`S3` | S0 原始/S1 守恒1/S2 守恒2/S3 对称守恒 |
| `jacobian_scheme` | string | 雅可比计算 `V1`/`V2`/`V3` | |
| `mid_metric_method` | string | 半点度量方法 `DEER`/`CMM` | |
| `inter_scheme` | string | 半点插值 `FirstOrder`/`MUSCL`/`MUSCL_SV`/`WCNS5`/`Grad` | |
| `flux_difference_scheme` | int | 通量差分 2/4/6 阶 | |
| `metric_difference_scheme` | int | 度量系数差分 2/6 阶 | |
| `spaceOrder` | int | 空间精度 1/2 阶 | |
| `limiterType` | string | 限制器 `noLimiter`/`barth`/`vk`/`1st-order` | |
| `maxLimiterSlope` | double | 最大限制器斜率 0~1 | |
| `firstOrderSteps` | int | 前 N 步强制一阶格式 | 二阶格式启动稳健性 |
| `fluidType` | string | 气体类型 `perfect_gas` | |
| `riemannSolver` | string | 黎曼求解器 `VanLeer`/`HLLC`/`StegerWarming`/`Ausmpw`/`Roe` | |
| `HoleSolverType` | string | 洞节点求解 `FNFDM`/`IDW` | |
| `initFieldType` | string | 流场初始化 `FarFieldNoVelocity`/`FarFlow`/`Backup`/`Explosion` | |
| `backupFieldFolder` | string | 流场备份文件夹 | |
| `backupFieldFileName` | string | 流场备份文件名（续算用） | |
| `residualFileName` | string | 残差输出文件名 | |
| `resultFolder` | string | 结果输出文件夹 | |
| `forceFileName` | string | 气动力文件名 | |
| `cfl_min` / `cfl_max` | double | CFL 数下/上限 | 随迭代由小到大增长 |
| `cfl_grow_step` | int | CFL 从最小增长到最大的步数 | |
| `cfl_reduce_factor` | double | 出现非物理值时 CFL 缩减因子 | |
| `checkField` / `stopWhenFieldError` | int | 是否检查流场 / 出错是否停止 | |
| `currentIter` | int | 当前迭代步（续算时读取） | |
| `currentTime` / `endTime` | double | 当前/结束物理时间 | |
| `dt` | double | 当前时间步长 | |
| `calResidualIter` | int | 计算/输出残差的步数间隔 | |
| `writeFieldIter` | int | 备份流场的步数间隔 | |
| `minResidual` | int | 残差对数小于该值时停止（量级） | |
| `maxIter` | int | 最大迭代步数 | |
| `inflowMachNumber` | double | 来流马赫数 | |
| `inflowAttackAngle` / `inflowSlideAngle` | double | 攻角/侧滑角（弧度） | |
| `inflowDensity` | double | 参考密度 kg/m³ | |
| `inflowTemperature` | double | 参考温度 K | |
| `inflowGamma` | double | 比热比 | |
| `inflowMw` | double | 分子量 mol/kg | |
| `refLength` | double | 参考长度 m | |
| `modelFileName` | string | 模型文件（STL/PLY，扎染法用） | |
| `xMin/xMax/yMin/yMax/zMin/zMax` | double | 扎染主网格范围 | |
| `nx`/`ny`/`nz` | int | 主网格单元数量 | |
| `boundTypeIMinus/IPlus/JMinus/JPlus/KMinus/KPlus` | string | 主网格六面边界类型 | |
| `projection_layer` | int | 扎染投影层数 | |
| `tol_factor` | double | 距离容差因子 | |

---

## 5. 场（Field）体系

### 5.1 `FieldManager`（`inc/Zone/Field/GlobalField.h`）

管理一个 `dynamic_array<shared_ptr<Field>>` 与对应的 `dynamic_array<shared_ptr<FieldDataCommInfo>>`，提供 `AddField`、`RemoveField`、`GetField`、`GetFieldNum`、`GetFieldDataCommInfo` 等接口。一个 `FieldManager` 可容纳多个场（如扎染法的主场与从属场、多块结构网格的各块）。

### 5.2 `Field` 基类（`inc/Zone/Field/Field.h`）

一个 `Field` 是“一个计算子域”的完整封装，包含五要素：

- `m_grid`：`shared_ptr<GridBase>` 网格
- `m_solver`：`shared_ptr<FieldSolver>` 求解器
- `m_solver_para`：`shared_ptr<SolverParam>` 求解参数
- `m_data`：`shared_ptr<FieldData>` 场数据（原始量、守恒量、梯度等）
- `m_data_manager`：`shared_ptr<DataManager>` 数据管理器

`Allocate()` 依次调用 `AllocateFieldData()`、`AllocateSolver()`、`AllocateSolverPara()`、`AllocateDataManager()`（后三个为纯虚，由派生类实现）。

### 5.3 派生体系

```
Field
 └─ FieldNS（NS 场，增加 ResInfo 残差信息，GetSolverPara→FlowSolverParam）
     ├─ NSFieldFNFDM（自由节点有限差分场，FieldType::NS_FlexibleNode）
     │     solver = NSSolverFNFDM，data_manager = DataManagerNS
     ├─ NSFieldStruct（结构网格场，FieldType::NS_Structured）
     │     solver = NSSolverStructCMM / NSSolverStructDEER（由 mid_metric_method 决定）
     │     data_manager = DataManagerNSStruct，参数 = FlowSolverParamStruct
     └─ NSFieldZaran（扎染主场，FieldType::NS_Zaran）
           solver = NSSolverBlock（继承自 NSSolverStructDEER）
           data_manager = DataManagerNSStruct；另含 m_slave_field（NSFieldFNFDM 从属场）与 m_model_manager
```

- 每个 `FieldNS` 持有 `m_res_info`（`ResInfo`），记录每个方程的残差无穷范数与 L2 范数。
- 残差计算 `CalcResidual()` 在具体 Field 派生类中实现：遍历内部节点，对每个方程求 `max|res|`（InfNorm）与 `sqrt(Σres²/N)`（L2Norm），并记录最大残差节点的坐标/索引。

### 5.4 `FieldGenerator` 工厂（`src/Generator/Field/FieldGenerator.cpp`）

`Create()` 根据 `gridType` 分支：

- **Zaran**：走 `CreateFieldZaran()`——读取 STL/PLY 模型 → 建 `ModelManager` → 用 `GridGeneratorBlock` 生成正交主网格 `GridBlock` → 建 `NSFieldZaran` 主场 → `CreateSlaveField()` 生成从属自由节点场 → 建立主从场间通信信息。
- **Flexible**：`GridGeneratorFlexible` 读取 `node.dat`/`cell.dat`/`bound.dat` 生成 `GridFN` → 建 `NSFieldFNFDM`。
- **Structured**：`GridBuilderStructGridgen` 读取 Gridgen 多块结构网格 → 建 `NSFieldStruct`，并处理块间 `connection` 边界建立块间通信信息。

每个场创建后调用 `Allocate()` 分配内存，并用 `FieldDataCommInfo` 建立场间数据交换。其中 Flexible/Structured 路径默认交换 `primitive_0..4` 五个原始变量；Zaran 路径在主从场之间额外交换 `conservative_`、`limiter_` 与 `prim_grad_`（5 个方程 × 3 个梯度方向）等共 30 个字段，实现主从场的全量耦合。

---

## 6. 网格（Grid）体系

### 6.1 继承体系

```
GridBase（name、index、dim、GridType）
 ├─ GridStruct（结构网格：NodeStruct/FaceStruct/CellStruct + BoundStructManager + IdProxyStruct）
 │    └─ GridBlock（扎染主网格，额外含 dx/dy/dz、bound_box、IBlank 标记数组）
 └─ GridFN（自由节点有限差分网格：NodeFN/FaceFN/CellFN + BoundManagerFN）
```

`GridType` 枚举：`Structured` / `Flexible` / `Zaran` / `Block` / `Unkown`。

### 6.2 节点（Node）

- `NodeBase`：仅记录节点总数。
- `NodeStruct`：按 `(i,j,k)` 存储坐标（`m_coord` 一维数组按 x,y,z 顺序），通过 `GetIdx(i,j,k)` 转一维。
- `NodeFN`（`NodeFNFDM.h`）：存储
  - `m_coordinate`：坐标（x,y,z）
  - `m_type`：`NodeType` 枚举（`inner=-1`、`inlet`、`outlet`、`wall`、`hole`、`symmetry`、`farfield`、`corner`、`ghost`、`undefined`）
  - 邻居信息（CSR 压缩存储）：`m_node_num`/`m_node_offset`/`m_node_idx`——**前 6 个邻居为差分模板**（i-/i+/j-/j+/k-/k+ 方向），其余为补充邻居；另有邻居面 `m_face_idx`、邻居单元 `m_neighor_cell`。

### 6.3 单元与面

- `CellFN`：记录单元包含的节点、面，以及单元中心坐标（CSR 存储）。
- `FaceFN`：记录面元包含的节点（`m_face2node`）、左右单元（`m_face2cell`）、面积（`m_area`）、法向（`m_normal`）——用于气动力积分。
- `CellStruct` / `FaceStruct`：结构网格对应的单元/面（按 i,j,k 索引）。

### 6.4 索引代理（IdxProxy / StructIdxProxy）

- `IdProxy`：通用一维索引代理（边界检查）。
- `IdProxyStruct`：三维结构网格 `(i,j,k) ↔ 一维 idx` 互转（`operator()(i,j,k)` 与 `GetIdxStruct(idx,i,j,k)`），列优先（k 最外）。

### 6.5 边界数据结构

- **自由节点**：`BoundManagerFN` 以 `map<string, dynamic_array<BoundFN>>` 按边界名（`riemann`/`inlet`/`outlet`/`wall`/`hole` 等）组织；`BoundFN` 记录边界节点索引 `idx_bound`、参考内点 `idx_ref`、ghost 节点 `idx_ghost` 与边界法向 `m_norm_bound[3]`。
- **结构网格**：`BoundStructManager` 以 `map<string, dynamic_array<BoundStruct>>` 组织；`BoundStruct` 记录源块边界 `(i,j,k)` 与方向 `dir_src`、法向 `norm`，以及块连接目标块的 `(i,j,k)`、方向 `dir_tgt`、`target_block`——支持多块结构网格的 `connection` 类型边界。

---

## 7. 数据管理（FieldData / DataManager）

### 7.1 `FieldData`（`inc/Zone/Data/FieldData.h`）

一个**通用、按名字索引**的数据容器：内部维护 `map<string,int> m_name_id`（名字→索引）、类型数组、大小数组，以及 `dynamic_array<dynamic_array<std::byte>> m_data`（原始字节存储）。接口：

- `AddData(name, type, num)`：登记一块名为 `name`、类型为 `real`/`integer`、长度为 `num` 的数据。
- `GetData(name, T*&)` / `GetData<T>(name)` / `GetData(name, index)`：取数据指针或元素引用。

这样把“命名逻辑”与“物理存储”解耦，便于统一输出（后处理按名字遍历输出所有量）与场间通信（按名字复制）。

### 7.2 `DataManagerNS`（自由节点）

`DataManagerNS` 继承 `DataManager`（`FieldDataManager`），把 `FieldData` 中登记的各字段绑定到 C 风格指针数组，供求解器高效访问。`CreateData()` 注册的字段命名（`equ_num = 5`）：

- `primitive_{0..4}`：原始变量（ρ, u, v, w, p）
- `conservative_{0..4}`：守恒变量
- `conservative_old_{0..4}`：上一时间步守恒量（Runge-Kutta 用）
- `residual_{0..4}`：残差（右端项）
- `limiter_{0..4}`：限制器系数
- `viscous_flux_{i}_{d}`：粘性通量（i=方程，d=方向）
- `prim_grad_{i}_{d}`：原始变量梯度（i=方程，d=梯度方向）
- `viscous_flux_grad_{i}_{d}_{g}`：粘性通量梯度
- `temperture_grad_{d}`：温度梯度
- `dt`：当地时间步长
- `temperature`：温度
- `non_physical`：非物理点标记（整数）

`DataManagerNSStruct` 在此基础上额外注册半点存储：`midnode_prim_left_{eq}_{dim}` / `midnode_prim_right_{eq}_{dim}`（半点左右重构值）与 `midnode_flux_{eq}_{dim}`（半点数值通量缓冲），用于结构网格有限差分的插值与通量差分。

`RegisterData()` 把上述字段指针绑定到成员数组 `m_prim/m_cons/m_cons_old/m_residual/m_limiter/m_dt/...`。`DataManagerNS` 提供 `SetPrim/SetCons/GetPrim/GetCons/GetPrimGrad/GetLimiter/...` 等读写接口。

变量索引常量定义在 `CommonPara.h`：`ID_DENSITY=0, ID_VELOCITY_X=1, ID_VELOCITY_Y=2, ID_VELOCITY_Z=3, ID_PRESSURE=4`。

### 7.3 `DataManagerNSStruct`（结构网格）

与 `DataManagerNS` 类似，但数据按结构网格节点组织（`ni×nj×nk`），通过 `IdProxyStruct` 用 `(i,j,k)` 访问；残差计算时还会除以时间步（`res/dt`）作为收敛判据。

### 7.4 `FieldDataCommInfo` — 场间数据通信

用于块间/主从场间的数据交换，记录：

- 源（接收方）节点索引 `src_data_idx`
- 目标场索引 `tgt_field_idx` 与目标节点索引 `tgt_node_idx`
- 需要交换的数据名列表 `recv_data_name`
- 源数据缓冲 `src_data_buffer`

`NSFieldSimulation::CommFieldData()` 分两阶段完成通信：第一阶段把每个“接收节点”所需的数据从对应“目标场/节点”读入缓冲；第二阶段把缓冲写回接收节点。扎染法中主场（Block）与从场（FN）即通过 `primitive_/conservative_/limiter_/prim_grad_` 等字段的通信实现耦合。

---

## 8. 求解器（Solver）体系

### 8.1 继承体系

```
Solver（id、name、grid、para；Init/Solve/Preprocess/Postprocess/InitSolver 虚接口）
 └─ FieldSolver（增加 DataManager；InitField/BoundaryCondition/UpdateField/BackupField 虚接口）
     └─ FlowFieldSolver（流场基类；InitField/InitSolver/Solve/Postprocess、TimeAdvance、ComputeCFL）
         └─ NSSolver（NS 方程求解器）
             ├─ NSSolverFNFDM（自由节点有限差分）
             └─ NSSolverStruct（结构网格）
                 ├─ NSSolverStructCMM（CMM 半点度量）
                 └─ NSSolverStructDEER（DEER 离散等价方程）
                     └─ NSSolverBlock（扎染主网格块求解器）
```

### 8.2 `NSSolver`（`src/Zone/Solver/NSSolver.cpp`）

核心流程骨架：

- `Init()`：`InitSolver()`（创建气体模型 `PerfectGas` 与黎曼求解器 `RiemannSolver` 工厂）→ `InitField()`（按 `initFieldType` 初始化流场）→ `CalcCoordTransCoef()`（计算坐标变换/度量系数）。
- `InitField()`：支持 `FarFlow`（均匀来流）、`FarFlowNoVelocity`（静止）、`Backup`（读备份续算）、`Explosion`（局部高压爆轰），初始化后 `Prim2Cons()`。
- `Preprocess()`：步数 +1 → `BoundaryCondition()`（施加边界条件）。
- `Solve()` → `TimeAdvance()` → `RungeKutta()`（显式时间推进）。
- `Postprocess()`：`UpdateField()`（Cons2Prim）→ `CheckPrimtive()`（检查非物理值）→ `FixPrimtive()`（修正）→ `CalcForce()`（气动力，当前多数为空实现）。
- `CalcResidual()` 模板：`ZeroResidual()` → `CalcConvectionResidual()` → `CalcViscousResidual()` → `CalcSourceResidual()`。

时间推进接口：`CalcTimeStepLocal()`（当地时间步）、`CalcMinTimeStep()`（全局最小时间步）、`ReduceTimeStep()`（同步为全局时间步）、`RungeKutta()`。

### 8.3 `NSSolverFNFDM`（自由节点有限差分求解器）

关键实现（`src/Zone/Solver/NSSolverFNFDM.cpp`）：

1. **度量系数** `CalcCoordTransCoef()`：对每个内部节点，取前 6 个差分模板邻居（i-/i+/j-/j+/k-/k+），用 `Metric::CalcMetric()` 计算逆度量系数 ξ/η/ζ 与雅可比 J。
2. **预处理** `Preprocess()`：`NSSolver::Preprocess()`（边界条件）→ `CalcPrimGrad()`（梯度：WLS 最小二乘 / UFDM / 不计算）→ `CalcLimiter()`（限制器：VK / Barth / none / 一阶）。
3. **对流通量残差** `CalcConvectionResidual()`：对每个内部节点，沿每个维度方向做半点二阶重构（`MidPointReconstruct2ndOrder`，用梯度 + 限制器线性外插），调用黎曼求解器 `m_riemann_solver->Solver(riemann_para)` 得到通量，再按“左右通量差”累加：`res -= flux_right - flux_left`。
4. **时间步** `CalcTimeStepLocal()`：`Δt = CFL / (|u·ξ|+|u·η|+|u·ζ| + c(|ξ|+|η|+|ζ|)) / J`。
5. **Runge-Kutta** `RungeKutta()`：保存旧守恒量 → 对每个 RK 子步：施加边界条件 → 计算残差 → `U = α·U_old + β·U + γ·dt·J·Res`（α/β/γ 来自 `rk_coef`）。
6. **边界条件** `BoundaryCondition()`：按边界名分派 `BCInflow`（超声速入口）、`BCOutflow`（超声速出口，复制内点）、`BCWall`（滑移/无穿透壁面，速度法向分量为 0）、`BCFarfield`（黎曼不变量远场，按超/亚声速与流入/流出分支处理）。
7. **粘性通量** `CalcViscousFlux()`：由温度用 Sutherland 公式求粘性系数，用梯度构造粘性应力张量与热通量（当前 `CalcViscousResidual()`/`CalcViscousFluxGrad()` 内有 `return;` 提前返回，粘性项暂未启用）。

### 8.4 `NSSolverStruct`（结构网格求解器）

在结构网格上做有限差分（`src/Zone/Solver/NSSolverStruct.cpp` 及 CMM/DEER 派生），关键能力：

- **度量系数**：`CalcMetrics()` 按 `metrics_scheme` 分派 `CalcMetricsS0/S1/S2/S3`（原始形式/守恒形式1/守恒形式2/对称守恒 SCMM），并可选择 2/6 阶差分（`..._2nd`/`..._6th`）；`CalcJacobian()` 按 `jacobian_scheme` 分派 `V1/V2/V3`。
- **半点插值** `InterMidNodePrim_*`：`1st`（一阶）、`Grad`（梯度二阶）、`MUSCL`、`MUSCLSV`、`WCNS5`。
- **通量差分** `FluxDifference2nd/4th/6th`：中心差分求对流残差。
- **CMM vs DEER**（决定采用哪个派生类）：
  - `NSSolverStructCMM`：度量系数存储在**半点**上（`CalcMidNodeMetrics` 插值得到半点度量），计算半点数值通量时直接使用半点度量系数。
  - `NSSolverStructDEER`：度量系数存储在**节点**上，基于“离散等价方程及其离散等价准则（DEER）”，计算半点通量时使用离散点度量系数（`NSSolverBlock` 即继承 DEER，用于扎染主网格）。
- **边界条件**：除入口/出口/壁面/远场外，还实现对称面 `BCSymmetry`、涡 `BCVortex`、双马赫反射 `BCDoubleMach`。
- **粘性/源项**：`CalcViscousResidual/CalcViscousFlux/CalcViscousFluxGrad` 与 `CalcSourceResidual` 当前均为 `// todo` 空实现，粘性项与源项尚未启用（与自由节点求解器一致）。

### 8.5 `Metric` 类（`src/Zone/Solver/Metric.cpp`）

每节点存储 33 个 double（`m_metric_num=33`）：`X/Y/Z/T`（逆度量 x,y,z,t 各 4）、`Xi/Eta/Zeta/Tau`（度量系数各 4）、`Jacobian`（1）。`CalcMetric()` 由三根当地网格线（左右邻居坐标差）构造坐标变换系数矩阵，求逆得到度量系数与雅可比。

---

## 9. 数值方法

### 9.1 控制方程与物理模型

- 求解可压缩 Euler/NS 方程，`equ_num=5`（质量、三方向动量、能量）。
- **完全气体 `PerfectGas`**：
  - 声速 `c = sqrt(γ p / ρ)`（无量纲形式 `sqrt(γ p/ρ)`）
  - 压力 `p = ρ T / γ`（无量纲），温度 `T = γ p/ρ`
  - `Prim2Cons`：`(ρ,u,v,w,p) → (ρ, ρu, ρv, ρw, ρv²/2 + p/(γ-1))`
  - `Cons2Prim` 为其逆
  - 粘性：Sutherland 公式（`m_mu0·Southerland(T)`），层流 Prandtl 数 0.72、湍流 0.9
- **无量纲化 `Dimensionless`**：参考密度/比热比/分子量/长度/温度 → 参考速度（当地声速）、参考压力 `ρ∞a∞²`、参考时间、参考质量、参考雷诺数；提供 `GetXxxDL`（有量纲→无量纲）与 `GetXxxReal`（逆）转换。

### 9.2 空间离散（自由节点 FNFDM）

1. 对内部节点用 6 个差分模板邻居构造当地坐标变换（ξ/η/ζ 三根网格线），由 `Metric` 求度量系数与雅可比 J。
2. 用加权最小二乘 `GradWLSQ` 求原始变量梯度。
3. 计算限制器系数（`LimiterVK` / `LimiterBarth`），抑制二阶重构振荡。
4. 半点二阶重构：`q_L = q_L + 0.5·φ_L·∇q_L·Δr`，`q_R = q_R - 0.5·φ_R·∇q_R·Δr`。
5. 黎曼求解器求数值通量，通量差构成对流残差。

### 9.3 黎曼求解器（`RiemannSolver`）

抽象接口 `Solver(RiemannSolverPara& para)`，`RiemannSolverPara` 封装左右原始变量、比热比、能量、焓、声速、速度、法向、法向速度、马赫数及输出 `flux`。工厂 `RiemannSolverBuilder::CreateUnique()` 按 `RiemannSolverType` 创建（未知类型回退为 VanLeer 并告警）：

| 枚举 | 格式 | 实现状态 |
|------|------|----------|
| `VanLeer` | Van Leer 通量矢量分裂（按马赫数分裂，固定 γ=1.4） | 已实现 |
| `HLLC` | HLLC 接触间断格式（Einfeldt 波速 + 修正声速 + 四分支） | 已实现 |
| `Roe` | Roe 近似黎曼求解器（Roe 平均 + Harten 熵修正 δ=0.01） | 已实现 |
| `StegerWarming` | Steger-Warming 通量矢量分裂 | 空实现（TODO） |
| `Ausmpw` | AUSM+ 系列（AUSMPW） | 空实现（TODO） |

### 9.4 时间推进（显式 Runge-Kutta）

- 保存旧守恒量 `U_old`。
- 每个 RK 子步：施加边界条件 → 计算残差 `Res` → `U_new = α·U_old + β·U + γ·dt·J·Res`。
- RK 系数（`FlowSolverPara::Init`）：
  - 1 阶：`{1,0,1}`
  - 2 阶：`{1,0,1}`、`{0.5,0.5,0.5}`
  - 3 阶：`{1,0,1}`、`{0.75,0.25,0.25}`、`{1/3,2/3,2/3}`
- **时间步**：各节点计算当地时间步，全场取最小值 `dt_global`；非定常（`isSteady==0`）时把全场时间步统一为 `dt_global`（真实时间推进），并推进 `currentTime`；到达 `endTime` 时截断时间步。物理时间的累加由 `TimeCompute`（简单的时间累加器：start/end/current/delta）配合 `GlobalData` 中的 `currentTime/endTime/dt` 完成。

### 9.5 限制器（`src/Basic/Math/Limiter.cpp`）

提供 `LimiterNone`、`LimiterMinMod`、`LimiterVanLeer`、`LimiterMixMinModVanLeer`、`LimiterVanAlbada`、`LimiterBarth`（`min(1, Δ/Δ2)`）、`LimiterFirstOrder`（恒 0）、`LimiterVK`（Venkatakrishnan，带 eps 平滑参数）。

---

## 10. 完整求解流程（主循环时序）

`NSFieldSimulation::SolveField()`（`src/Main/FieldSimulation.cpp`）是核心控制器，流程如下：

```
Initialize()                 // 初始化 currentIter/currentTime，各场 solver->Init()
SaveFieldData()              // 输出初始流场（Tecplot）+ 备份
SaveResidual()               // 写残差文件表头
while (ContinueSolve()) {    // 判断是否继续
    currentIter++
    PreSolve()               // 各场 solver->Preprocess()（边界条件、梯度、限制器）+ CalcTimeStep()
    SolveOneStep()           // 各场 solver->Solve()（Runge-Kutta 时间推进）
    PostSolve()              // 各场 solver->Postprocess() + CommFieldData() + 残差/备份
}
PostSolve()
SaveFieldData()
```

各步骤细节：

- **`PreSolve()`**：`currentIter++`；对每个场调用 `solver->Preprocess()`（NS 求解器内部：步数+1、施加边界条件；FNFDM 还计算梯度与限制器）；随后 `CalcTimeStep()` 统计全局最小时间步并更新 `dt` 与 `currentTime`。
- **`SolveOneStep()`**：对每个场调用 `solver->Solve()`（即 Runge-Kutta 子步推进）。
- **`PostSolve()`**：对每个场 `solver->Postprocess()`（Cons2Prim、检查/修正原始变量、气动力）；然后 `CommFieldData()`（场间/块间数据通信）；每 `calResidualIter` 步调用 `CalcResidual()` 计算并写入残差（`iter, time, Res_Linf, Res_L2`）；每 `writeFieldIter` 步调用 `SaveFieldData()` 备份。
- **`ContinueSolve()`** 停止判据：
  1. 若 `log10(res_max) < -minResidual` 且 `currentIter > calResidualIter` → 停止；
  2. `currentIter > maxIter` → 停止；
  3. `currentTime ≥ endTime` → 停止。

---

## 11. 前处理与后处理

### 11.1 网格生成器（`src/Generator/Grid`）

| 生成器 | 输入 | 输出 |
|--------|------|------|
| `GridGeneratorFlexible` | `node.dat`（节点坐标）、`cell.dat`（单元）、`bound.dat`（边界） | `GridFN`（自由节点网格，含邻居排序、边界法向） |
| `GridGeneratorFlexibleSYSU` | SYSU 格式节点/单元/边界（已弃用） | `GridFN` |
| `GridBuilderStructGridgen` | Gridgen 多块结构网格（节点 + 边界/连接） | 多个 `GridStruct`（含 ghost 节点、块连接 `connection` 边界） |
| `GridGeneratorBlock` | `GridBlockInfo`（范围、nx/ny/nz、六面边界类型） | `GridBlock`（均匀正交结构网格，含 IBlank 标记） |
| `GridFNFactoryZaran` | `GridBlock` + `ModelManager`（STL/PLY 模型） | `GridFN`（扎染从属自由节点网格） |

扎染从属网格的生成要点（`GridFNFactoryZaran`）：先在主网格上 `TagCells` 标记单元（用 `ModelManager::GetClosestDistance` 判固体，洪泛法填流体，标记 `FluidSolid` 过渡单元），再在物面附近分层生成贴体节点——第 0 层为参考节点（过渡节点的内部镜像点）、第 1 层为过渡节点、末层为物面投影节点（`GetClosestPoint` 投影到模型表面）、中间层按几何级数（比例约 1.5）插值；最后 `BuildFNCell` 生成四边形（2D）/六面体（3D）贴体单元，并与主网格建立场间通信关系。

### 11.2 模型读取与 KDTree

- `STLReader`：基于 VTK `vtkSTLReader` 读取 STL 三角面片 → `PolyData`。
- `PlyReader`：手写解析 ASCII PLY 点云 → `PointCloudModel`。
- `PolyDataModel`：`SetPolyData(..., tol)` 合并顶点；内部用 VTK `vtkOBBTree`（内/外判断）、`vtkCellLocator`（最近点）与 `vtkImplicitPolyDataDistance`（有符号距离）；`IsClosed()` 用 `vtkFeatureEdges` 统计开放边/非流形边，数量为 0 即封闭。
- `PointCloudModel`：内部用 VTK `vtkKdTreePointLocator` 做最近点查询。
- `ModelManager`：聚合多个模型（OR 合并、取最小距离），提供包围盒 `GetBox()`、最近点/最近距离查询。
- 自研 `KDTree`（`inc/Basic/Utilities/KDTree.h`）：最近邻/半径查询，当前未被点云模型实际引用（对应 include 已注释，供后续替换 VTK 定位器使用）。

### 11.3 后处理 `Visual`（`src/Basic/PostProcess/Visual.cpp`）

按场类型重载，支持多种输出格式：

- `WriteTecplotBinary` / `WriteTecASCII`：Tecplot 二进制/ASCII（使用 TecIO 库）。
- `WriteVtkASCII` / `WriteVtkBinary`：VTK 文本/二进制（使用 VTK 库）。
- `WriteCGNS`：CGNS 格式（使用 CGNS/HDF5 库）。

当前主流程 `SaveDataTecplot()` 默认调用 `WriteTecplotBinary()`。

---

## 12. 运行方式与输入输出文件

### 12.1 运行方式

```text
Zaran3.10.1.exe                 # 使用当前目录下的 zaran.ini
Zaran3.10.1.exe <工作目录>       # 使用 <工作目录>\zaran.ini
```

### 12.2 输入文件

| 文件 | 说明 |
|------|------|
| `zaran.ini` | 控制文件（必选） |
| `node.dat` / `cell.dat` / `bound.dat` | 自由节点网格（`gridType=Flexible`） |
| Gridgen 节点/边界文件 | 结构网格（`gridType=Structured`） |
| `*.stl` / `*.ply` | 模型文件（`gridType=Zaran`） |
| `fieldBackup.dat` | 续算备份流场（`initFieldType=Backup`） |

### 12.3 输出文件

| 文件/目录 | 说明 |
|-----------|------|
| `log.txt` | spdlog 日志（追加模式） |
| `residual.dat` | 残差历史（`step, time, Res_Linf, Res_L2`） |
| `force.dat` | 气动力 |
| `resultFolder/` | Tecplot/VTK/CGNS 结果 |
| `backupFieldFolder/iter=<n>/` | 每 `writeFieldIter` 步备份的流场、残差、日志、控制文件（用于续算） |

---

## 13. 关键技术要点小结

1. **控制文件驱动的全自动流程**：所有运行参数集中在一个 `zaran.ini`，程序启动即自动完成网格生成、流场初始化、迭代求解与结果输出。
2. **工厂 + 多态分层**：`FieldGenerator` 创建场、`GridGenerator` 创建网格、`RiemannSolverBuilder` 创建通量格式，通过继承把“自由节点有限差分”“结构网格有限差分”“扎染浸入边界”三条技术路线统一到同一套 `Field/Solver/DataManager` 框架下。
3. **统一数据模型**：`FieldData` 以“命名字节块”存数据，`DataManagerNS(Struct)` 绑定为 C 指针，`FieldDataCommInfo` 负责场间/块间通信，使多块、主从耦合共享同一套数据交换机制。
4. **几何守恒与度量**：自由节点法通过 `Metric` 计算当地坐标变换度量；结构网格法提供多种度量（S0~S3）与雅可比（V1~V3）计算，并区分 DEER（节点度量）与 CMM（半点度量）两条路线，体现几何守恒律（GCL）与离散等价方程（DEER）思想。
5. **高精度格式与稳健性**：支持一阶/MUSCL/WCNS5/Grad 插值与 2/4/6 阶通量差分、VK/Barth 限制器、前 N 步一阶启动、CFL 逐步增长与出错缩减、非物理值检测与修正，兼顾精度与稳定性。

---

## 14. 参考资料

- [1] 刘君, 韩芳, 夏冰. 有限差分法中几何守恒律的机理及算法[J]. 空气动力学学报, 2018, 036(006): 917-926.
- [2] 刘君, 韩芳. 有限差分法中的贴体坐标变换[J]. 气体物理, 2018, 003(005): 18-29.
- [3] 陈洁. 基于三相邻节点的非结构网格有限差分方法[D]. 大连理工大学, 2019.
- [4] 刘君, 陈洁, 韩芳. 基于离散等价方程的非结构网格有限差分法[J]. 航空学报, 2020(1).
- [5] 刘君, 魏雁昕, 陈洁. 基于非结构网格有限差分法的扎染算法[J]. 航空学报, 2021, 42(07): 258-270.
